# Plan: <slug>

[1–2 sentences: what this plan accomplishes.]

## Goal

[What the plan delivers — the artifact, decision, or finding the analysis produces.]

## Constraints

[What NOT to do. Project-specific boundaries: panels not to break, deflator chains not to alter, sample frames to preserve.]

## Decisions Made

[Methodology calls already settled (typically from a brainstorm at `plan/brainstorms/<slug>.md`). Don't re-debate during execution. Cross-link `research/methods/<date>_<slug>.md` for peer-reviewable methodology calls.]

## File Manifest

[Paths + intent — what to create, modify, or delete and why. No code snippets. Use ✚ for new, ✎ for modify, ✘ for delete.]

## Repo Context

[How this plan fits into the project — relevant scripts (`scripts/NN_*.R` or `scripts/NN_*.py`), methods rules (`research/methods/<slug>/rule.md`), decision records (`research/methods/<date>_<slug>.md`), data sources (`research/sources/<source>.md`).]

## Phases

[Per-phase entries with intent, files modified, verification gates, and tasks-as-checkpoints. Verification is domain-shaped: sign-of-coefficients, magnitude sanity, source citation present, breakpoint alignment, row-count reconciliation. For multi-phase plans split each phase into `phases/phase-N.md`.]

## Phase Order + Dependencies

[Which phases depend on which. Sequential vs parallel. Strict ordering vs partial.]
